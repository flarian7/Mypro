#include<stdio.h>

int main(){
float Area,l,b;
printf("Enter the value of length:");
scanf("%f",&l);

printf("Enter the value of breadth:");
scanf("%f",&b);

Area = (l*b);
printf("%f",Area);


return 0;
}
